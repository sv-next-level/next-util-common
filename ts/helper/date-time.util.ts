export default class DateTIme {
  /**
   * 1. get current timestamp
   * 2. set start timestamp
   * 3. set end timestamp
   * 4. get time difference (end - start)
   * 5. static methods to format date
   * 6.
   */
}

/**
 * create new common database like redis to store and get the responses / create new service like next common(stack infra) and copy that repo befor building
 * store all responses in common mongo db and get all and store in ram while build time
 * store all common things in common db, which are same used in multiple services
 *
 */
